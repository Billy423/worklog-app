# Docker Compose variable substitution — copy to .env at project root
# These are read by docker compose itself, not the app container.

# PostgreSQL password (shared between postgres service and app DATABASE_URL)
POSTGRES_PASSWORD=
